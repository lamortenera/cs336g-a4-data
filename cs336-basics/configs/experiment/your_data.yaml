# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: ../data/leaderboard_testrun/tokenized.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin: ../data/paloma_c4_100_domains_val.bin
  
  model_output: ../data/leaderboard_testrun/model_output_old

model:
  context_length: 128

training:
  wandb_entity: ~  # Set to your wandb entity, e.g. pliang
  wandb_project: ~
